# RBK Command Center — Master TODO

> ⚠️ THIS IS THE ONLY TODO FILE. Update this after every session. Do not create new TODO or action item files. Archive any other planning docs by adding an ARCHIVED header. Phase build plans are reference-only.

*Last updated: September 3, 2026*

For the longer roadmap + horizon items + architecture history, see `RBKCC_VISION_AND_ROADMAP.md`. For implementation detail, see `CLAUDE_CONTEXT.md` ("Recent Changes" section).

---

## ✅ SCHOOL-YEAR MODE ACTIVE (restored 2026-09-02)

**The summer shutdown is over and everything syncs automatically again.** All 9 scheduled Cloud Functions are redeployed and ENABLED at their original crons; Cloud Run is back to `--min-instances=1 --max-instances=10 --no-cpu-throttling --memory=512Mi` (and `firebase.json` frameworksBackend `512MiB`). The `## SEPTEMBER RESTART CHECKLIST` section has been removed from `CLAUDE.md` now that it is complete — the record of what was restored is in the September 2 entry under Recent Changes there.

The two summer breakages are fixed: the in-app **"Sync Gmail"** and manual draft-sync buttons work again (their Pub/Sub topics were recreated with the schedulers — both HTTP functions now return `{"success":true}`), and `gifts_cache` + `constituents_cache` are refreshing on schedule.

### 🔴 Open — needs a human (I could not do these)

1. **~~Gmail refresh token is rejected~~ — RETIRED 2026-09-03 rather than fixed.** The All Emails module is now hidden from everyone except the super-admin and the `triageGmail` scheduler has been deleted, so this is no longer costing anything (it was ~96 failing invocations/weekday). **The token is still broken** — reopen this only if Gmail triage is wanted back. Original diagnosis: `triageGmail` fires on schedule and completes `ok`, but the Gmail API returns `403 PERMISSION_DENIED — "Delegation denied for rglassberg@saracademy.org"`. The single workspace with a `gmail_refresh_token` ("SAR Academy — RBK", owner `kraussb@`) has a token that resolves to Becca, which Google won't delegate to RBK's mailbox. `syncDraftsReady` shares that token and fails the same way. **Fix: RBK (`kraussb@saracademy.org`) signs in and re-runs `/api/auth/gmail-consent`.** It must be RBK — `auth/gmail-callback` only overwrites the workspace-level token when the connecting user is a workspace *owner*, so Becca re-consenting will not help. Context: the newest row in `emails` is 2026-08-06, so triage has been down since well before this restart; whether the delegation error predates the shutdown could not be determined from available logs.
2. **Veracross Data Package cadence** (restart checklist step 5) — it was moved to weekly, Sunday 3am for the summer and should go back to its school-year schedule. That setting lives in the Veracross admin UI, not in this repo or GCP.
3. **Buzz morning briefing has not been smoke-tested.** Deliberately not triggered during the restart: `DRY_RUN` is false, so it would have sent real Slack DMs to Becca and Emily at 7pm for no reason. Its hard dependency (`--no-cpu-throttling`) is restored and verified; the `30 7 * * 1-5` scheduler will exercise it at 7:30am on the next school day. **Check that it actually sent** before assuming Buzz is healthy.

Still billing (deliberately not touched): the **SFTP VM** (`veracross-sftp`, verified RUNNING in `us-east1-c`), Supabase, and the Veracross Data Package. The SFTP VM is the largest line item if savings are ever wanted again.

---

## ✅ Shipped (September 3, 2026)

- ✅ **`syncDraftsReady` scheduler disabled — no Gmail-dependent scheduler remains** (functions-only deploy, `npx firebase deploy --only functions --force`; **no new Cloud Run revision**, app code untouched, hosting still on `ssrrbkcmdcenter-00732-kt9`). Closes the loose end flagged in the triageGmail retirement below. `exports.syncDraftsReady` commented out (handler preserved as `syncDraftsReadyHandler`) and the function deleted from GCP. Rationale: the GAS `syncDraftsReadyLabel()` covers this flow, and the Cloud Function was failing on every run for the same reason triageGmail was — but **silently**: it reported `status: 'ok'` in ~1s while logging `Failed to create label "Drafts Ready"` → `No "Drafts Ready" label, skipping`, ~96 no-op invocations/weekday. (Lesson: a green `ok` was hiding a total no-op.) **State now: 7 schedulers, all ENABLED, zero Gmail-dependent ones.** Side effect is smaller than triageGmail's — deleting the topic makes `manualSyncDrafts` return `5 NOT_FOUND`, but **nothing under `app/` calls `/api/gmail/sync-drafts`**, so no user-facing button is affected (grep finds only the route's own logging); route + function are now orphaned, left in place for a one-line re-enable. tsc clean (app + functions); module-load pre-flight shows 10 exports, `syncDraftsReady` absent.
  - **⚠️ Follow-up for whoever owns Drafts Ready: the pipeline looks dormant on BOTH sides.** Disabling the Cloud Function was correct regardless, but the data does not show GAS producing either — **43 rows sit open at `draft_status='draft_ready'`, but the newest `updated_at` on any of them is 2026-06-08** (~3 months), and the newest `emails` insert is 2026-08-06. If Emily is meant to be receiving Drafts Ready items, **check the Apps Script triggers for `syncDraftsReadyLabel` are actually enabled** — likely the same broken Gmail credential. This change did not cause it, but it removes the last in-repo fallback.

- ✅ **Gmail triage retired — inbox hidden from non-super-admins, `triageGmail` scheduler deleted** (firebase release 2026-09-03 01:05 UTC; **Cloud Run revision `ssrrbkcmdcenter-00732-kt9`**). RBK stopped using Gmail triage. `Sidebar.tsx`: the "All Emails" nav item is now spread in conditionally on `isRealAdmin` — gating on **super-admin, not role**, because RBK is `role='owner'` and an owner/assistant check would not have hidden it from him; `isRealAdmin` (not the bare `isSuperAdmin` prop) matches this file's other admin gates so Becca can't lock herself out on a pre-`is_super_admin` cookie. `Dashboard.tsx`: added a one-shot landing guard — `activeNav` is persisted to the URL hash, so anyone whose last session ended on the inbox would reload straight back into a page with no sidebar entry; the guard corrects only the **initial URL-derived** nav for non-super-admins (via a new `initialNavFromUrlRef`, since the hash-sync effect makes URL-supplied and in-app navs indistinguishable after mount) and waits on `workspaceId` so it doesn't bounce the super-admin before `isSuperAdmin` resolves. **In-app navigation to the inbox still works** — the Drafts Ready flow (Tasks "Drafts to Approve" + Dashboard drafts cards) calls `setActiveNav('inbox')` and is explicitly meant to keep working. `functions/src/index.ts`: `exports.triageGmail` commented out (handler preserved) and the function **deleted from GCP** — note `./deploy.sh` aborts on deletions in non-interactive mode, so it needed a follow-up `npx firebase deploy --only functions --force`. 8 schedulers remain, all ENABLED. tsc + build clean; Cloud Run settings verified intact post-deploy (`timeout=300s` survived the hosting step, confirming the previous day's `deploy.sh` pin works).
  - **⚠️ Expected side effect (verified):** deleting the scheduler destroys the Pub/Sub topic `manualTriage` publishes to, so the in-app **"Sync Gmail" button now 500s** (`5 NOT_FOUND: Resource not found`). Acceptable — that button is on the now-hidden All Emails page — but it means only Becca can reach it and it will fail for her. `manualTriage` is effectively dead code, left in place so re-enabling is one uncomment.
  - **~~🔴 Flagged, deliberately NOT changed — `syncDraftsReady` is failing every 15 min too.~~ ACTIONED same day (see the entry above).** Original finding: The task scoped this to `triageGmail` on the basis that Drafts Ready runs in Google Apps Script independently. True of the **GAS** `syncDraftsReadyLabel()`, but there is *also* a **Cloud Function** `syncDraftsReady` on the same `*/15 * * * 1-5` schedule sharing the same broken token: it completes `ok` but logs `Failed to create label "Drafts Ready"` → `No "Drafts Ready" label, skipping` and does nothing. Same wasted-resources argument applies. Left enabled because it was out of scope and disabling it could affect Emily's workflow if GAS isn't actually carrying it. **If the intent was "stop all failing Gmail schedulers", it's the same one-line change in the same file.**

## ✅ Shipped (September 2, 2026)

- ✅ **September restart — all 9 schedulers re-enabled, Cloud Run back to school-year settings** (firebase release 2026-09-02 19:00; **Cloud Run revision `ssrrbkcmdcenter-00729-zld`**). Worked the SEPTEMBER RESTART CHECKLIST end to end and deleted it from `CLAUDE.md`. Restored at original crons (all `America/New_York`, all verified ENABLED): `scheduledMorningBriefings` 30 7 * * 1-5, `syncDailyAttendance` 0 23 * * 1-5, `syncAfterSchoolPrograms` 0 7 * * *, `syncGiftsHourlyWeekdays` 0 * * * 1-5, `syncGiftsDailyWeekends` 0 6 * * 0,6, `dailyTaskDueReminder` 0 8 * * *, `dailyAbsenceAlert` 30 9 * * 1-5, `triageGmail` */15 * * * 1-5, `syncDraftsReady` */15 * * * 1-5. **Note there is no separate `syncConstituents` scheduler** — constituents sync is chained inside `runGiftsSyncForAllWorkspaces()`, so the two gifts schedulers cover it. Cloud Run: `--min-instances=1 --max-instances=10 --no-cpu-throttling --memory=512Mi`, summer's explicit `--cpu-throttling` deleted, `firebase.json` → `512MiB` (verified live: cpu-throttling false / minScale 1 / maxScale 10 / 512Mi). **Verified:** gifts sync run via the real scheduler job took FY27 `Operating 2026-2027` from **62 gifts/$191,397.70 → 63/$191,433.70**; `constituents_cache` refreshed 2026-07-25 → today; `veracross-sftp` already RUNNING; both manual Gmail buttons back to `{"success":true}` 200. tsc clean (app + functions); site 307/200/200/401.
  - **⚠️ Re-enabling was NOT "purely uncommenting"** as the shutdown note claimed. `triageGmailHandler` / `syncDraftsReadyHandler` are `const` arrow functions declared *below* their export blocks, so `.onRun(triageGmailHandler)` would throw a temporal-dead-zone `ReferenceError` at module load and fail the deploy. Both are now wrapped as `.onRun(async () => { await handler(); return null; })` to defer the lookup. Cheap pre-flight: `require()` the compiled `lib/index.js` with `GCLOUD_PROJECT`/`FIREBASE_CONFIG` stubbed — it surfaces this before deploying.
  - **⚠️ Fixed a real bug the restart exposed:** both gifts schedulers had no `runWith`, so they inherited the 1st-gen **60s default timeout**. The gifts leg alone takes ~58s, so the hourly job often died mid-run and the chained constituents sync **never completed** — `constituents_cache` had not refreshed since 2026-07-25, quietly staling the roles behind Development Overview segment classification. Added `functions.runWith({ timeoutSeconds: 540 })`; re-run went from `60000 ms status: 'timeout'` → **`94437 ms status: 'ok'`** with the constituents leg finishing. Pre-existing fragility, but without this the automation was only nominally restored.
  - **⚠️ Second timeout bug (found 2026-09-03 by watching the first unattended run): Cloud Run's REQUEST timeout was 60s.** Even after the function timeout fix, the 00:00 unattended run failed its constituents leg at exactly 60.0s — `FetchError: invalid json response body … "upstream r"…`, i.e. a plain-text `upstream request timeout` from the gateway that the caller then tried to JSON-parse. The constituents sync takes ~47-60s on live data, so it was succeeding or failing at random. **Root cause: `firebase deploy --only hosting` recreates the SSR Cloud Run service with the frameworks default 60s, silently reverting the manual `--timeout=300` a prior session applied.** Fixed by pinning `--timeout=300` in `deploy.sh`'s `gcloud run services update` block (which runs after the hosting step, so it can't be reverted). Verified: next run `143502 ms status 'ok'`, constituents leg 86s, both legs success. **Takeaway: any Cloud Run setting not in `deploy.sh` will not survive a deploy.**
  - **⚠️ `.DS_Store` build race, worse form:** `rm -rf .next` itself failed (`rm: .next/server: Directory not empty`) because Finder rewrote a `.DS_Store` mid-delete. Robust fix is to rename first — `mv .next .next.trash.$$ && rm -rf .next.trash.$$` — since `mv` is atomic and Finder isn't watching the renamed path.


- ✅ **FY27 rollover — Development Overview moved to `Operating 2026-2027`** (firebase release 2026-09-02; **Cloud Run revision `ssrrbkcmdcenter-00726-4wz`**). The fiscal year turned over Sept 1. Current campaign is now FY27 `Operating 2026-2027`; FY26 `Operating 2025-2026` is prior year; FY25 `Operating 2024-2025` is two-years-ago context. `FY27_CAMPAIGN` is the **only** hardcoded string — FY26 and FY25 derive from it via the existing `priorCampaign()` helper, so **next September is a one-line bump**. What shipped: (1) segment cards report FY27 and each carries a muted `FY26` prior-year reference line, computed by running the **identical** donor→segment pipeline over FY26 (same twin-matching soft-credit gap-fill, same board_member_overrides, same "Other" DAF dedup — extracted into a shared `buildSegmentTotals()` helper so the two years can't drift methodologically); (2) **Campaign Giving by Fund is now three columns + change** — FY27 | FY26 | FY25 | CHANGE (FY27 vs FY26), with FY25 rendered lighter as context and the delta/percent computed server-side; (3) lapsed/new/retained rebased to FY27-vs-FY26; (4) the segment-donors drilldown follows to FY27 so it still reconciles with the card it was opened from. **Untouched per spec:** soft-credit gap-fill dedup, board_member_overrides, the "Other" softCreditedOrgs exclusion, Israel/Cooper/Guardian Circle/Weekly Gifts, and the giving_history_cache FY25 fund data. **Deliberate deviation from the brief:** the FY26 lapsed/new baseline **unions** `giving_history_cache` with `gifts_cache` instead of preferring history with a zero-count fallback — history only reflects the last nightly CSV drop, gifts_cache covers FY26 completely, and preferring history alone would shrink the baseline and wrongly promote real FY26 donors into "New Donors". A union can only grow the baseline and degrades to exactly the gifts_cache set when history is empty (the brief's fallback is the union's limiting case). Measured: gifts_cache 2,046 / history 2,044 / union 2,048. **Verified by SQL:** FY27 `SUM(amount)` type-1/2 = **$191,397.70** across 62 gifts ✓; 63 distinct FY27 donors (types 1/2/3); 43 retained, 20 new, 2,005 lapsed. Two notes on those numbers: the headline card reads **$189,857.70**, not $191,397.70, because the headline uses the Veracross "Donation & Pledge Balance" formula (type-1 `amount` + type-2 **`pledge_balance`**) and one FY27 pledge is $1,540 paid down — correct long-standing behavior, not a regression; and the very large lapsed count is expected two days into a fiscal year (it means "gave FY26, not yet FY27" and drains toward the card's "Goal: 0 by Aug 31"). tsc + build clean; deployed (Cloud Run `00726-4wz` 100%, site 307, both overview routes 401 = auth-gated and alive).
  - **⚠️ Deploy gotcha worth remembering:** `npm run build` fails with `ENOTEMPTY: … rmdir '.next/server'` when Finder has dropped a `.DS_Store` into a `.next` subdirectory since the last build. Retrying `./deploy.sh` does not help. **Fix: `rm -rf .next && ./deploy.sh` in one command.** The first failed attempt left a dead Cloud Run revision (`00724-7cz`) and the known stale-image error on the forced revision update; both self-healed on the clean rerun and `00723-8v6` served throughout — no downtime.

- ✅ **Docs: project path updated `~/Desktop/DevProjects` → `~/Projects/DevProjects`** (no deploy — documentation only). The project folder moved. Updated the 4 markdown files that referenced the old path: `CLAUDE.md` (the two `_context/` pointers) and the three `RBKCC_HANDOFF_*.md` files (Local Project line). Replaced the directory prefix rather than the literal whole path, so `~/Desktop/DevProjects/_context/LHASA_CONTEXT.md` correctly became `~/Projects/DevProjects/_context/LHASA_CONTEXT.md` instead of being flattened to the repo root. `RBKCC_MASTER_TODO 6:5:26.md` had **no** references to the old path. One reference deliberately left alone: `CLAUDE.md` line 4's "(repo root + `~/Desktop/`)" — that describes a copy of the handoff sitting on the Desktop itself, which is a different location from the moved `DevProjects` folder and outside the scope of the move.

## ✅ Shipped (August 10, 2026)

- ✅ **FIX: After School sync — session-auth fallback for the manual UI trigger** (firebase release 2026-08-10; **Cloud Run revision `ssrrbkcmdcenter-00723-8v6`**). The **Sync** button on the After School Programs page always returned `Unauthorized` — `app/api/after-school/sync/route.ts` only accepted the `x-internal-secret` shared secret (for the `syncAfterSchoolPrograms` Cloud Function), and the browser sends a `__session` cookie instead. With all schedulers deleted for the summer, that button was the only remaining way to refresh after-school data, so the module was frozen. Applied the exact dual-auth pattern from `app/api/development/giving-history/ingest/route.ts`: accept **either** `X-Internal-Secret` = `INTERNAL_SYNC_SECRET`/`SYNC_SECRET` (Cloud Function, unchanged) **or** an authenticated session where the caller is `is_super_admin` **or** `role === 'owner'` (manual trigger). Secret is checked first; `getAuthSession()` only runs on fallthrough, so the Cloud Function path is unchanged. **Deviation from ingest (deliberate, per spec):** ingest gates on `sessionIsSuperAdmin` alone, this route also allows `role === 'owner'` — so **RBK can run it, not just Becca**. No middleware change needed: the route was already in the `middleware.ts` exclusion list, which only bypasses the middleware session *check*; `getAuthSession()` reads the cookie inside the handler (same as ingest). **Untouched** per spec: all other after-school routes, all sync/Veracross/upsert logic in the file, `AfterSchoolTab.tsx`, the Cloud Function. tsc + build clean; deployed (Cloud Run `00723-8v6` 100%, site 307). Verified: unauthenticated POST → 401, wrong-secret POST → 401 (fallback rejects, doesn't open the route); the owner-session path needs a real browser to exercise.

## ✅ Shipped (July 25, 2026)

- ✅ **Summer shutdown — minimum-cost mode + all scheduled Cloud Functions disabled** (firebase release 2026-07-25; **Cloud Run revision `ssrrbkcmdcenter-00721-hfr`**). App stays deployed and reachable at minimum cost until September. **`deploy.sh`:** `--min-instances=1`→`0`, new `--max-instances=2`, `--memory=512Mi`→`256Mi`, `--no-cpu-throttling` removed, plus a `# SCHOOL YEAR: restore …` comment above the `gcloud run services update` block; `firebase.json` `frameworksBackend.memory` → `256MiB`. **⚠️ Non-obvious finding:** dropping `--no-cpu-throttling` does NOT revert it — CPU allocation is a sticky service setting, and Cloud Run hard-rejects `<512Mi` while CPU is always-allocated (`400 … Total memory < 512 Mi is not supported with cpu always allocated`). An explicit **`--cpu-throttling`** flag had to be added; in September, memory must go back to `512Mi` in the same command as re-adding `--no-cpu-throttling`, and `--cpu-throttling` must be deleted. A second failure (stale `:version_1` image not found) self-healed on a full `./deploy.sh` rerun that rebuilt the image; revision `00718-kzz` kept serving throughout, **no downtime**. **All 9 schedulers deleted** from GCP via `npx firebase deploy --only functions --force` (`scheduledMorningBriefings`, `syncDailyAttendance`, `syncAfterSchoolPrograms`, `syncGiftsHourlyWeekdays`, `syncGiftsDailyWeekends`, `dailyTaskDueReminder`, `dailyAbsenceAlert`, `triageGmail`, `syncDraftsReady`) — `gcloud scheduler jobs list` now returns 0 items. Handler **bodies preserved** as `…Handler` functions/consts so September is pure uncommenting; the two big ones use `const xHandler = (async () => {…});` to keep bodies byte-identical. The 3 HTTP functions (`manualTriage`, `manualSyncDrafts`, `generateMorningBriefings`) kept + redeployed. `ingestGivingHistory` skipped (already retired 2026-06-25, manual-only). Verified: serving revision `256Mi` / `cpu-throttling: true` / minScale 0 / maxScale 2, 0 scheduler jobs, site 307; `tsc` + build clean. **➡️ Restart steps live in `## SEPTEMBER RESTART CHECKLIST` in `CLAUDE.md`.**

## ✅ Shipped (July 21, 2026)

- ✅ **`is_super_admin` flag — Becca (system admin) vs RBK (owner)** (firebase release 2026-07-21e; **Cloud Run revision `ssrrbkcmdcenter-00718-kzz`**). New `workspace_members.is_super_admin` (true only for rglassberg@) now plumbed through the session cookie → `lib/auth.ts` (server `sessionIsSuperAdmin(session)` helper) + `AuthProvider` (`useWorkspace().isSuperAdmin`, client). Admin gating moved from hardcoded `ADMIN_EMAIL` → super-admin across: /admin/permissions page + all its backing APIs (workspace-members, workspace, integrations/+test/+migrate, feature-flags), Development "Sync Gift History" (button + ingest route), online-gifts-diagnostic, and the Sidebar Admin link + impersonation. **RBK (owner, not super-admin) can no longer access the admin panel, sync gift history, or impersonate — only Becca.** Enrollment-projection `isAdmissionsManager` rebased owner→super-admin (RBK now hidden from locked Projection like a viewer). ⚡SA badge added in the permissions By-User list + detail header; super-admin row protected from role-change/removal. ADMIN_EMAIL kept ONLY as a transitional fallback for Becca's own account (no lockout on stale cookies; removable after sessions rotate). Left as-is (out of scope): `lib/impersonate.ts` + morning-briefing routes (allow owners intentionally). RBK's role stays owner; module access + Emily-assists-RBK untouched. tsc + build clean; deployed (site 307, admin APIs 401).

- ✅ **Admissions: `admissions_manager` permission + granular Projection lock visibility + enrollment edit mode** (firebase release 2026-07-21d; **Cloud Run revision `ssrrbkcmdcenter-00716-4gf`**). New client flags `isAdmissionsManager` (owner or `admissions_manager` sub-perm) + `canEditEnrollment` (manager or `edit_enrollment_budget`/`edit_enrollment_data`). When Projection is **locked**, only admissions managers see the Projection segment button + banner + unlock; everyone else sees only Current Enrollment. When **unlocked**, all admissions users see it. Lock/unlock buttons + the PATCH `/api/admissions/current-enrollment` gate moved owner-only → owner-or-`admissions_manager`. Inline edit for `canEditEnrollment`: **Pisgah** column in Current Enrollment is click-to-edit → saved to `workspace_settings` key `pisgah_counts` via new `/api/admissions/enrollment-settings` route (GET/PATCH, permission-gated); projection **Budgeted** edit gate widened to `canEditEnrollment` (still saves to the existing enrollment_budget table). Permissions page: `admissions_manager` added to `SUB_PERMISSIONS.admissions` (shows in By-User + By-Module panels). **⚠️ Follow-up:** Emily Daniel, Debra Eis + 5 other admissions viewers currently have legacy `admissions: true` (grants ALL sub-perms incl. admissions_manager); admins should uncheck `admissions_manager` in the permissions UI for anyone who shouldn't have it (converts to nested form). Only `Dashboard.tsx`, `permissions/page.tsx`, `current-enrollment/route.ts` + new route touched. tsc + build clean; deployed (site 307).

- ✅ **Current Enrollment: grade drilldown → right-side slide-in panel** (firebase release 2026-07-21c; **Cloud Run revision `ssrrbkcmdcenter-00714-zh9`**). Clicking a grade row in the Current Enrollment view previously expanded the student list inline under the By Grade table; it now opens the standard Admissions right-side panel (backdrop click-to-close + `max-w-[420px]` slide-in, header "{Grade} — {N} students", round X-close, scrollable body of name pills with Pisgah badge + Veracross student-ls link). Uses the existing `currentEnrollmentDrilldownGrade` state; panel is `position: fixed` so nesting is irrelevant. Untouched: Current Enrollment data/route, the grade table, all other drilldown panels. Only `Dashboard.tsx`. tsc + build clean; deployed (site 307).

- ✅ **FIX: Current Enrollment showed 0 for all grades — wrong Veracross data source** (firebase release 2026-07-21b; **Cloud Run revision `ssrrbkcmdcenter-00712-hzh`**). The `academics/enrollments` endpoint returns COURSE enrollments (grade in `grade_level_id`, not `grade_level`) and is incomplete for Academy in July, so the route dropped every row → 0. Rewrote `/api/admissions/current-enrollment` to source the 26-27 headcount from the `/v3/students` roster (grade_level, admissions numbering, active-only) — matches `/api/admissions` `currentYearCounts` (academy total ≈ 1036). Added `[CURRENT-ENROLLMENT]` client + route logging. Diagnosed via live Veracross probe. tsc + build clean; deployed (site 307).

- ✅ **Admissions → Enrollment Projection: Current-Enrollment view + Projection year-lock toggle** (firebase release 2026-07-21; **Cloud Run revision `ssrrbkcmdcenter-00708-tfg`** — deploy completed non-interactively via the ADC token, see below). After Veracross rolled the school year over, `/v3/students.grade_level` shows each student's NEXT-year (27-28) grade while nobody has registered for 27-28 yet, so the projection tab was misleading. The tab now has a segmented **[ Current Enrollment ] [ Enrollment Projection ]** toggle (defaults to Current every open) + a dynamic school-year header. **Current Enrollment** (always on) — "Current Enrollment — 2026-27 School Year"; new `GET /api/admissions/current-enrollment` reads Veracross `academics/enrollments?school_year=2026` for the authoritative current grade, joins `/v3/students` only for stable name/campus/Pisgah fields (its grade is post-rollover and unusable), dedups by person_id; UI shows ELC/Lower/Middle cards + Total + a headcount-only By Grade table (Grade | Enrolled | Progress | School Level | Pisgah) with a click-to-expand student-name drilldown (each links to the Veracross student-ls page). **Enrollment Projection** (existing content unchanged) — locked/grayed behind a banner ("Re-enrollment for 2027-28 opens in January 2027…") until an **owner** clicks "Unlock Projection Mode" (owner-only `PATCH /api/admissions/current-enrollment` → `workspace_settings.enrollment_projection_enabled`); owners can re-lock. **New generic `workspace_settings` table** (migration `scripts/migrations/workspace-settings.sql`, applied to Supabase `ftjppqvxthxcvhokrhfw`; RLS on, service-role only; SAR row seeded `false`) — this is the project's first workspace feature-flag store; use it (not the credentials-only `workspace_integrations`) for future flags. **Untouched** per spec: existing projection columns/cards/funnel/drilldown, Admissions Overview, New Enrollment, Pisgah manual entry, all other routes. tsc (exit 0) + build (exit 0) clean; deploy shipped (SSR `Successful update operation`, hosting released, Cloud Run `00708-tfg` 100% traffic, site 307). **DEPLOY NOTE / carryover:** the interactive `gcloud auth print-access-token` still needs `gcloud auth login` reauth, BUT `gcloud auth application-default print-access-token` works — so `./deploy.sh` now runs fully non-interactively by exporting `CLOUDSDK_AUTH_ACCESS_TOKEN="$(gcloud auth application-default print-access-token)"` first (that captured revision 00708-tfg). **Follow-ups:** (1) spot-check the `academics/enrollments` field shape / `school_year=2026` behavior against Cloud Run `[current-enrollment]` logs on first real use (handled defensively but unverified — no live session); (2) Emily/Becca unlock Projection mode in Jan 2027 via the in-app owner button (no deploy needed); (3) Pisgah on current students is best-effort from the roster `student_group` string.

## ✅ Shipped (July 16, 2026)

- ✅ **FY25 Campaign Giving by Fund → giving_history_cache with new `fund` column** (firebase release 2026-07-16; Cloud Run revision # unread — gcloud reauth still pending). The Veracross gift-history CSV now has a Fund column; `giving_history_cache` gained `fund text`. **Parser** (`lib/parseGivingHistoryCSV.ts`): `GivingHistoryRow.fund` read from col 12 (0-indexed 11, appended-last **assumption** — couldn't read the GCS object locally, `rglassberg@` ADC lacks `storage.objects.get`; **verified correct post-sync** — fund populated with real `OP: …` names). **Ingest**: added `fund` to the upsert. **Overview** (`overview/route.ts`): new paginated `giving_history_cache` query (`fundraising_activity='Operating 2024-2025'`, gift_type 1+2, ~3.2k rows) builds FY25 per-fund totals; campaigns array uses `fundFY25Final = history ?? gifts_cache fallback`. Deviation: no `pledge_balance` in history → FY25 type-2 uses face-value `amount` (FY25 is a historical reference; FY26 untouched, still reconciles with headline). **Re-sync ran:** 23,147 parsed / 22,137 upserted / 1,010 skipped (`Operating_Gift_History.csv`, 2026-07-16); FY25 Guardian Circle now $4.2M (complete). Untouched: FY26 campaign logic, segment/headline totals, soft-credit gap-fill, all other routes. tsc + build clean; firebase shipped (site 307, overview 401). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers/logs are readable; re-confirm the Fund column position against the live CSV once bucket read access is restored.

## ✅ Shipped (July 14, 2026)

- ✅ **@Notify — interactive Slack "Mark Resolved" button + Command Center link** (firebase release 2026-07-14e; Cloud Run revision # unread — gcloud reauth still pending). The group DM now shows two buttons: **Open in Command Center** (url button → `/?nav=tasks`) and **✓ Mark Resolved** (interactive). New `POST /api/slack/interactions` route handles the `block_actions` payload: resolves the clicking Slack user → member, finds their open `source='notify'` task on the message's thread (`slack_thread_ts` = button message ts, precise across reused MPIM channels), marks it done, and posts `✓ <name> marked this resolved` into the thread. Each member resolves only their own task; always acks 200 so Slack won't retry; url button interaction ignored. Added to middleware auth exclusion. **STEP 2D — Slack signature verification:** `verifySlackSignature` (HMAC-SHA256 `v0=` over `v0:{ts}:{body}` with `process.env.SLACK_SIGNING_SECRET`, length-guarded `timingSafeEqual` + 5-min replay guard); **non-fatal when the secret is unset** (skip + warn), 401 once it's set and a signature is bad. Untouched: morning briefings, Buzz, task-assignment DMs, the @Notify group-DM open/post flow, all other routes. tsc + build clean; firebase shipped (site 307; unsigned interactions POST acks 200). **⚠️ MANUAL SETUP NEEDED:** api.slack.com/apps → Command Center app → Interactivity & Shortcuts → ON → Request URL `https://ssrrbkcmdcenter-429508710310.us-east1.run.app/api/slack/interactions` → Save. Optionally add `SLACK_SIGNING_SECRET` to Cloud Run to secure it. **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

- ✅ **@Notify Slack group DM — diagnostic logging + flow verified** (firebase release 2026-07-14d; Cloud Run revision # unread — gcloud reauth still pending). The group DM was silently failing (`slack_thread_ts`/`slack_channel_id` null on all notify tasks) because Slack errors went to `console.warn` truncated to 300 chars. Added loud `[NOTIFY SLACK ERROR]` (`console.error`) logging with the **full** untruncated Slack responses + `needed`/`provided` scope fields in `openGroupDm`/`postSlackMessage` (`lib/slackNotifications.ts`), plus route-level logging in `app/api/notify/route.ts` (participant Slack IDs, count, token presence, who was dropped for no `slack_user_id`, and a distinct error branch per failure stage: no token / `<2` participants / openGroupDm null / postSlackMessage null). **Verified the openGroupDm flow is already correct** — conversations.open with a comma-separated string → `channel.id` → chat.postMessage with that channel — so no flow fix was needed; only observability. **Next step (needs user/gcloud):** trigger an @Notify and read Cloud Run logs (`… logs read ssrrbkcmdcenter --region=us-east1 … | grep 'NOTIFY SLACK'`); likely `missing_scope`/`needed: mpim:write` (stored bot token predates the reinstall → reconnect Slack in Admin → Integrations) or `<2 participants with a Slack ID` (sender/tagged member lacks `slack_user_id`). Couldn't read logs this run — gcloud not authed. tsc + build clean; firebase shipped (site 307, `/api/notify` 401). **TODO carryover:** run `gcloud auth login` so `./deploy.sh` finishes fully + logs/revision #s are readable.

- ✅ **@Notify follow-ups — Tasks visible to all users + Admissions note pre-fill** (firebase release 2026-07-14c; Cloud Run revision # unread — gcloud reauth still pending). **FIX 1:** the Sidebar Daily section (incl. Tasks) is hidden for viewers-with-`allowed_modules`, so they couldn't reach their @Notify "Needs Your Reply" tasks. Added a standalone Tasks nav block in `Sidebar.tsx` for exactly that case (just Tasks, not Inbox/Agenda/Compose). Add-User form now defaults `addModules` to `{ tasks: true }` (init + `resetAddForm`) so new viewers get tasks automatically; the permissions Tasks toggle is otherwise unchanged (admins can still uncheck). **FIX 2:** Admissions candidate NotifyButton now pre-fills with the note text via a ref-based bridge — `DonorAnnotations`/`NotesList` gained optional `onDraftChange` (fired on draft change → written to `admissionsNoteDraftRef` in Dashboard, no re-render), and `NotifyButton` gained optional `getMessage()` read on open. Note-save and notify stay two separate actions; dev call sites unaffected. Untouched: @Notify Slack DM logic, task creation/resolve loop, other modules. tsc + build clean; firebase shipped (site 307); gcloud finalize aborted on reauth (redundant). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

- ✅ **This Week at SAR — iframe URL → Vercel** (firebase release 2026-07-14b; Cloud Run revision # unread — gcloud reauth still pending). `ThisWeekCard.tsx` `IFRAME_URL`: `thisweek-sar.netlify.app` → `this-week-at-sar.vercel.app` (calendar embed changed hosts). Feeds the `<iframe src>` + the two "open in new tab" links. **Deploy was required** despite being a one-line change — it's a `'use client'` component so the URL is bundled into the client JS at build time; the live site serves the pre-built bundle, so `npm run build` + `./deploy.sh` were needed for it to take effect. Committed separately (`Update This Week at SAR link…`). Build clean; firebase shipped (site 307); gcloud finalize aborted on reauth (redundant). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

- ✅ **Cross-module @Notify — Slack group DM + task creation with reply loop** (firebase release 2026-07-14; Cloud Run revision # unread — gcloud reauth still pending). New `@` NotifyButton lets any workspace member tag ≥1 teammate, fire a Slack **group DM** (sender + tagged + each tagged member's assistant, deduped by slack_user_id), and drop a `tasks` row on each tagged member's list. When a resulting task is marked done, `PATCH /api/tasks` posts `✓ <name> marked this resolved` back into the Slack thread. **New:** `app/components/shared/NotifyButton.tsx` (self-contained popover: message textarea + searchable multi-select person picker w/ role badges + chips), `POST /api/notify` route, `openGroupDm` + `postSlackMessage` helpers in `lib/slackNotifications.ts`. **Migration** `scripts/migrations/tasks-slack-thread.sql` (applied live): added `tasks.slack_thread_ts` + `slack_channel_id`, dropped the `tasks_assigned_to_check` CHECK (was hard-limited to the 5 assignee keys; @Notify assigns to any member by `assignee_key ?? display_name`). **Dashboard:** content-area NotifyButton at top-right of every module (context by `activeNav` via `NOTIFY_CONTEXT_LABELS`), a "Needs Your Reply" sourced-task group in the Tasks column (source='notify', matched by assignee_key OR display_name), and per-candidate NotifyButton in all 7 Admissions drilldown Notes panels (context "Admissions: <name>"). `mentionable-users` route extended to return `role`. **Scope deviations (documented in CLAUDE.md):** Step 4 = one content-area button per module (not 13 header edits); Step 5 pre-fills context but not note-text (lives in DonorAnnotations internal state); `source_ref`=context, `source`='notify' (no context_label/task_type columns); **viewers** (most tagged members) don't have the Tasks page in their sidebar so they currently only get the Slack DM — an in-app resolve surface for viewers is a follow-up. tsc + build clean; `./deploy.sh` firebase step shipped (SSR `Successful update operation`, hosting released; site 307, `/api/notify` 401=auth). gcloud finalize step aborted on reauth (redundant). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable. **Follow-up:** surface notify tasks in a viewer-accessible list; optionally pre-fill the Admissions per-candidate NotifyButton message with the live note text.

## ✅ Shipped (June 28, 2026)

- ✅ **"Other" segment — exclude family foundations/DAFs already represented via soft credits** (firebase release 2026-06-28b; Cloud Run revision # unread — gcloud reauth still pending). Supersedes the earlier same-day grants-only filter. "Other" was double-counting foundations/DAFs whose direct gift is also soft-credited to a named-segment individual (Luxenberg Chesed Fund $99k in Other while the Luxenbergs get the GC soft credit in Grandparent). New rule (`overview/route.ts` + `segment-donors/route.ts`): record each FY26 type-1/2 gift's `id`→owner (`type1GiftToConstituent`); after the loop build `softCreditedOrgs` = owners whose gift has a FY26 type-3 soft credit from a DIFFERENT constituent; exclude `cid` from Other when `segmentOf === 'Other' && softCreditedOrgs.has(cid)`. Removed the prior `GRANTS_FUND`/`softFunds` check + the now-unused `fund` select in segment-donors (overview keeps `fund` for the campaign table). Verified live (SQL): excluded — Lieberman Charitable Fund ($101,800), Conduit Foundation ($100,000), Luxenberg Chesed Fund ($99,000), Lent Tzeddakah Fund ($85,000), Milstein DAF ($80,000); still in Other — UJA-Federation ($70,500, grants-only, no household soft credit). Untouched: named-segment classification, headline totals, soft-credit gap-fill dedup, Board priority/overrides, lapsed/new/retained, all other tabs/routes. tsc + build clean; firebase shipped (site 307, both routes 401). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

- ✅ **"Other" segment grants-only filter + Board Members drilldown secondary-role summary** (firebase release 2026-06-28; Cloud Run revision # unread — gcloud reauth still pending). Heidi QA. **(1)** "Other" was double-counting role-less records whose soft credits came from named funds (Guardian Circle, Anniversary Dinner, Shavuot Appeal). Now an Other-classified donor is included only if their qualifying FY26 soft credits are all from `OP: Grants` (or they have none — direct gifts only); any non-grants soft credit excludes them from Other. Applied in `overview/route.ts` (Other card total + count) and `segment-donors/route.ts` (Other drilldown) via a new per-donor `softFunds: Set<string>`; gated to `segment === 'Other'` so named segments are untouched. Verified `OP: Grants` matches the data exactly (1 grants-only donor; 1,342/1,343 FY26 soft-credit donors carry a named-fund soft credit). **(2)** Board Members drilldown (`OverviewTab.tsx`) gained a summary bar above the list: one SEGMENT_STYLE-colored pill per `secondaryRole` group showing total giving + trustee count, sorted by total desc, Board-Members-only. Untouched: segment classification, headline totals, Board priority/overrides, lapsed/new/retained, soft-credit dedup-by-hcid, all other tabs/routes. tsc + build clean; firebase shipped (site 307, both routes 401). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

## ✅ Shipped (June 25, 2026)

- ✅ **Overview gifts query scoped to FY25+FY26 exactly (FY27-pledge leak defense)** (firebase release 2026-06-25c; Cloud Run revision # unread — gcloud reauth still pending). Reported: Operating 2026-2027 pledges leaking into FY26 segment totals (Grunberg 10958, Fleisch 6757, $10k each). **Investigation found neither overview route was actually leaking** — the overview loop already buckets with exact `activity === FY26_CAMPAIGN`, and segment-donors already queries with exact `.eq(FY26_CAMPAIGN)`. SQL-simulated the route math: Grunberg FY26 = $25,000, Fleisch = $16,264.17, both already excluding the $10k FY27 pledge. The only prefix match was the overview *query's* `.ilike('Operating %')` (needed to also pull FY25); hardened to `.in([FY25_CAMPAIGN, FY26_CAMPAIGN])` so FY27+ rows are never fetched (defense-in-depth; byte-identical output, FY25 preserved). Removed unused `OPERATING_CAMPAIGN_PREFIX`. segment-donors unchanged (already exact). Untouched: giving_history FY25 baseline, headline totals, Board Members, soft-credit gap-fill, all other routes. tsc + build clean; firebase shipped (site 307, both routes 401). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

- ✅ **Giving-history ingest manual-only + "Sync Gift History" button** (firebase release 2026-06-25b; Cloud Run revision # unread — gcloud reauth still pending). Retired the nightly auto-ingest: deleted the scheduled `ingestGivingHistory` Cloud Function (daily 8am ET) from `functions/src/index.ts` and from GCP via `npx firebase functions:delete ingestGivingHistory --region us-central1 --force` (confirmed gone via `functions:list`). The ingest route + GCS pipeline are unchanged — now triggered only by the admin (rglassberg@) from the Development Overview. That trigger upgraded from a tiny "Import History" link to a bordered **"↓ Sync Gift History"** button (spinner while "Syncing…") with a **persistent** result line (no auto-dismiss): `✓ Synced {N} records from {file} · file dated {modified}` on success, `✗ {message}` in red on error. Ingest route now returns `file_modified` (the CSV's GCS `metadata.updated`, ISO) in success + upsert-error payloads. Untouched: ingest logic, every other Cloud Function/scheduler, other tabs/routes, SFTP VM + GCS bucket. tsc (app+functions) + build clean; firebase shipped (site 307, ingest 401). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

- ✅ **Soft-credit gap-fill twin-matched — DAF/foundation soft credits now count alongside direct gifts** (firebase release 2026-06-25; Cloud Run revision # unread — gcloud reauth still pending). The Development Overview segment cards + drilldown dropped foundation/DAF money for any donor who ALSO gave directly: the old rule counted soft credits ONLY when the donor had no type-1 gift. New rule (`overview/route.ts` + `segment-donors/route.ts`): a soft credit is an excluded Veracross twin **only** when its `hard_credit_gift_id` matches the record `id` of one of that constituent's own type-1 gifts; otherwise it came from a separate source (their foundation gave directly) and is counted. `received = SUM(type-1 amount) + SUM(non-twin soft-credit amount)`. Per-donor accumulator now tracks `type1GiftIds` (from each type-1 row's `id` — type-1 rows have NULL hard_credit_gift_id) + `softByKey` (deduped by hcid). Soft pool stays gift_type=3 (sc 1/2) — type-5 is a separate representation of the same pledge and would double-count. **Verified live (SQL):** Luxenberg 17707 = **$101,360** (was $2,360), Tsigutkin 9458 = **$55,000** (was $5,000); workspace-wide **131 donors gain +$827,885** (those who give both directly + via DAF/foundation; min +$54, max +$99k). Untouched: headline Total Raised/Donors, lapsed/new/retained counts (the "gave" sets are provably unchanged), Board Members priority/overrides, all other tabs/routes. **Spec deviation (documented):** the brief said to build `type1GiftIds` from type-1 rows' `hard_credit_gift_id`, but those are NULL in gifts_cache — implemented the prose intent ("own type-1 gift IDs") by collecting the record `id` instead. tsc + build clean. firebase shipped (site 307, both routes 401). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

## ✅ Shipped (June 23, 2026)

- ✅ **Admissions applicant Veracross links → admission-candidate page** (firebase release 2026-06-23; Cloud Run revision # unread — gcloud reauth still pending). In the Admissions module, the 11 Veracross deep-links now route applicants (not-yet-enrolled) to `…/detail/admission-candidate/{id}/3052-general` and enrolled / re-enrolling students to the existing `…/detail/student-ls/{id}/273-general`. New module-scope helper `veracrossAdmissionUrl(personId, isEnrolled)` in `Dashboard.tsx`. Classification: `a.applicant_id` rows (pending/waitlist/declined/overview applicants) → admission-candidate (isEnrolled=false); `ReEnrollmentStudent`/`reEnrollmentsData` `s.id` rows (notReEnrolling, projection_leaving) → student-ls (isEnrolled=true); `.source`-bearing rows (filteredCity drilldown, Pisgah, registration-incomplete, projection_combined/category) → `isEnrolled = source === 're'`. Default is admission-candidate for anyone not confirmed enrolled. **Untouched** per spec: `AfterSchoolTab.tsx` student-ls link + every non-Admissions Veracross link; only `Dashboard.tsx` changed. tsc + build clean. **Deploy:** firebase step shipped the code (SSR `Successful update operation`, hosting released; site 307, overview 401); deploy.sh gcloud-only steps aborted again on `gcloud auth login` reauth (redundant this run). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

## ✅ Shipped (June 19, 2026)

- ✅ **Board member overrides — joint-record trustees classified correctly** (firebase release 2026-06-19; Cloud Run revision # unread — gcloud reauth still pending). Trustees who give via a joint household record (no "Trustee" in their own roles_raw) were landing in Parent/Parents-of-Alumni/Alumni. New `board_member_overrides` table (created+seeded externally; 6 SAR rows) is now read in `overview/route.ts` + `segment-donors/route.ts`: `segmentOf(cid)` returns "Board Members" if cid is in the override set, else the existing `classifySegment`. Trustee roles_raw check untouched (still handles the majority). Board Members card got a ❓ tooltip; secondaryRole unchanged (Trustee-strip is a no-op for override-only members → real segment). DDL saved to `scripts/migrations/board-member-overrides.sql`. Verified all 6 IDs have FY26 Operating gifts + no Trustee in roles_raw. Untouched: other segments, lapsed/new/retained, other tabs. tsc + build clean. **Deploy:** firebase step shipped cleanly (SSR updated, hosting released; site 307, env vars intact); deploy.sh gcloud steps still blocked on `gcloud auth login` reauth (redundant this run). **TODO carryover:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

## ✅ Shipped (June 18, 2026)

- ✅ **New Donors fix — prior-year baseline keys on campaign, not fiscal_year** (firebase release 2026-06-18 21:31 ET; Cloud Run revision # unread — see deploy caveat). Three long-time donors (Newman 5673, Landes 439, Perl 8610) showed as New. Root cause: the overview lapsed/new/retained baseline queried `giving_history_cache` by `fiscal_year='FY25'`, but Veracross stamps `fiscal_year` from gift DATE — so Operating 2024-2025 gifts dated in the FY24 (Aug 2024) or FY26 (Sep 2025) shoulder windows were missed. Compounding: `gift_type IN (1,2,3)` dropped type-4 pledge installments (Landes/Perl's only FY25-stamped rows). **Fix:** baseline now uses `fundraising_activity='Operating 2024-2025'` (= `priorCampaign(FY26_CAMPAIGN)`, date-independent, mirrors the FY26 side) + gift types 1-5. FY26 side, segment totals, headline, soft-credit gap-fill unchanged. Verified at data layer: all 3 now in baseline (~1,943 donors types 1-3 / 1,948 types 1-5). tsc + build clean. **Deploy caveat:** `./deploy.sh` aborted at its first gcloud step — gcloud token needs interactive `gcloud auth login` reauth. The `firebase deploy` step (separate auth) ran first and released the new SSR code (21:31); post-deploy checks confirm the fix is live (site 307, ingest secret works ⇒ env vars intact, IAM intact). Skipped gcloud steps were redundant this run. **TODO:** run `gcloud auth login` so future `./deploy.sh` runs complete fully + revision numbers are readable.

- ✅ **giving_history_cache GCS ingest — RE-VERIFIED, no redeploy** (live stays rev `ssrrbkcmdcenter-00690-5b6`). A re-request of the 2026-06-17 GCS ingest pipeline; on inspection it was already fully shipped + live, so no code changed and no new revision was cut. Verified: `@google-cloud/storage` present, all artifacts present (table, parser, `/ingest` route, `ingestGivingHistory` fn w/ scheduler ENABLED, overview FY25 `fy25Baseline` + gifts_cache fallback, admin Import button, middleware exclusion), `git status` clean, tsc clean. Re-ran the live ingest to refresh data → 22,941 parsed / 21,999 upserted / 942 skipped (bucket CSV unchanged); FY25 baseline 1,942 donors. Skipped a redundant no-op redeploy (no code delta); spec's `fy25BaselineDonors` is `fy25Baseline` in shipped code (functionally identical).

## ✅ Shipped (June 17, 2026)

- ✅ **Board Members drilldown — secondary-role badge per trustee** (rev `ssrrbkcmdcenter-00690-5b6`, deployed + site UP HTTP 307). In the Board Members segment drilldown only, each donor row shows the role they'd classify as without "Trustee" (Parent/Grandparent/etc.) as a colored pill next to the name. `segment-donors/route.ts` adds `secondaryRole` (via `classifySegment` on a Trustee-stripped roles_raw; null for other segments); `OverviewTab.tsx` renders it with existing `SEGMENT_STYLE` colors, gated to the Board Members drawer. No other drilldown/total/classification/priority/route changed. tsc + build clean.

- ✅ **giving_history_cache — GCS ingest pipeline (replaces Gmail import) + lapsed/new fix LIVE** (rev `ssrrbkcmdcenter-00688-wkw`, deployed + site UP HTTP 307). Veracross drops the nightly Operating gift-history CSV via SFTP to `gs://rbk-cmd-center-sftp/veracross/giving-history/`. Installed `@google-cloud/storage`; new `POST /api/development/giving-history/ingest` reads the newest CSV from GCS (ADC / Cloud Run SA), parses, dedups by gift_record_id, batched upsert. **Replaced the prior Gmail approach** — deleted the `import` route + `watchGivingHistoryExport` function (via `firebase functions:delete`), added `ingestGivingHistory` Cloud Function (8am ET, scheduler ENABLED). Granted `roles/storage.objectViewer` to the compute SA on the bucket (required for download). **Ran live: 22,941 parsed / 21,999 upserted; FY25 baseline now 1,942 distinct donors (was ~965)** → long-time donors no longer false-"New". Overview FY25 baseline reads giving_history_cache w/ gifts_cache fallback; UI "Import History" button repointed to `/ingest`. Real export is ~23k rows (not the ~185k assumed). Untouched: gifts_cache sync, segment/headline totals, soft-credit gap-fill, other tabs. tsc (app+functions) + build clean. **Note:** supersedes the 2026-06-16 Gmail version (which had landed but the email never came — Veracross used SFTP instead).

## ✅ Shipped (June 16, 2026)

- ✅ **giving_history_cache — Veracross "Operating Gift History Export" CSV import pipeline** (rev `ssrrbkcmdcenter-00686-gtz`, deployed + site UP HTTP 307). New `giving_history_cache` table (all-years Operating gifts, ~185k rows; RLS server-only) + `lib/parseGivingHistoryCSV.ts` (RFC-4180 parser, gift-type/date/FY/amount normalization) + `POST /api/development/giving-history/import` (reads Becca's Gmail for the nightly "Operating Gift History Export", downloads+parses the CSV attachment, dedups by gift_record_id, batched upsert of 500; X-Internal-Secret OR admin auth) + daily-8am-ET `watchGivingHistoryExport` Cloud Function (scheduler ENABLED) + admin-only "Import History" button on the Overview tab. Overview lapsed/new/retained FY25 baseline now reads `giving_history_cache` (FY25, gift_type 1/2/3) with a gifts_cache fallback when empty; FY26 stays gifts_cache. **Verified live:** endpoint + Gmail auth work (clean 404 until the first export email arrives; overview falls back safely). Untouched: gifts_cache hourly sync, segment card totals, headline totals, all other tabs. tsc (app+functions) + build clean. **Pending (external):** Veracross must start sending the nightly export email; the manual button / 8am function will populate the table once it arrives.

- ✅ **Dev batch — segment dedup by hard_credit_gift_id + drilldown/tooltip/Israel/Weekly/Cooper fixes** (rev `ssrrbkcmdcenter-00684-f2n`, deployed + site UP HTTP 307; 3 commits). **(1)** Segment soft-credit dedup now keys on `hard_credit_gift_id` (not amount) in overview + segment-donors routes — fixes Cory Greenbaum's 11×$540 soft credits showing $540 instead of $5,940 (verified live: 11 distinct gift ids → $5,940); also fixes Alumni drilldown "one gift" report. Permissions: category labels Academics/Operations/Community/Productivity + By-Module alphabetical-within-category sort (By-User order already matched spec). **(2)** New Donors drilldown already org-excluded; added FY-labeled tooltips to Lapsed/New pills (no Retained card exists to attach one — skipped); Israel Fund "Total Raised" card now clickable → inline raised-by-initiative list (uses existing `moneyIn` payload); segment drilldown per-donor totals fixed by the dedup. **(3)** Weekly Gifts excludes internal "SAR Academy" entries via `/sar.*academy/i`; Lapsed + New count pills now exclude orgs (match drilldowns); Cooper Fund event merges (Schreck/Yahrzeit/General→Cooper 25-26, drop Sayeret Matkal — mostly pre-existing) + pie chart switched to value-bearing vertical legend (no overlapping labels). Untouched: segment card totals, Israel Fund sync, headline Total Raised/Donors. tsc + build clean.

- ✅ **Admin permissions — Users tab redesigned (By User / By Module)** (rev `ssrrbkcmdcenter-00682-vv2`, deployed + site UP HTTP 307). Replaced the flat module-toggle table with two sub-tabs under a shared search. **By User**: scrollable user list (role-colored avatars, +Add User modal) → detail panel (editable name, role 3-button toggle, divisions, title, Slack ID read-only, assistant picker, category-grouped module toggles w/ sub-permission expanders for viewers, 🧪 Testing & Preview, Remove). **By Module**: category-badged module cards w/ live access counts → right slide-in panel (every member toggle — owners/assistants disabled-on, viewers interactive — + per-viewer sub-permissions). New `MODULE_CATALOG` covers all `ALL_MODULE_OPTIONS` keys + `student_logs`; SCHOOL/OPERATIONS/COMMUNITY/CORE grouping. **Only the Users tab changed** — School Settings / Integrations / Feature Flags, page path, "Admin" title, `ADMIN_EMAIL` guard untouched. No API/`lib/modules.ts` changes; reused existing handlers + `isModuleEnabled`/`hasSubPermission`. Preserved display-name edit + assistant picker (the only admin UI for `assistant_to`). Added `slack_user_id` to client `Member` type (route already returned it). tsc + build clean.

## ✅ Shipped (June 15, 2026)

- ✅ **Development Overview — lapsed/new/retained soft-credit gap-fill** (rev `ssrrbkcmdcenter-00680-c6b`, deployed + site UP HTTP 307). Follow-up to `00678-q2c`. The "gave" sets were `gift_type IN (1,2)` only, so soft-credit-only constituents (gave via DAF/foundation/household — the fund carries the type-1 row, the person carries only `gift_type=3` rows) were mis-classified. Applied the SAME gap-fill rule the segment cards use: a constituent **"gave in a FY"** if they have type-1 OR type-2 OR `gift_type=3` w/ `soft_credit_type IN (1,2)` and no type-1 row. **Corrects:** *Engelhardt, Nachi and Samantha* (FY26 soft credits only → was wrongly **lapsed**, now **retained**) and *Schanzer, Bruce and Jill* (cid 12103, FY25 soft credits only, fund 15018 gave directly → was **missing from lapsed list**, now correctly **lapsed**). **Impl (overview route only):** FY26 soft branch now adds to `fy26GaveDonors` + records `lastFy26SoftByDonor`; new FY25 soft branch adds to `fy25GaveDonors` + records `lastFy25SoftByDonor`; `buildDonorList` resolves last gift as `direct ?? soft` (gap-fill). Broadening the two gave sets fixes all three ID sets (lapsed/new/retained), both count pills, and both drilldown lists at once. **`segment-donors/route.ts` NOT touched** — the lapsed/new lists live in the main overview payload, not segment-donors. **Unchanged:** headline Total Raised/Donors, segment card totals + dedup logic, Board Members priority, FIX 2/3/4 from `00678`. **Campaign-by-Fund table deliberately NOT given gap-fill** (re-confirmed: must reconcile with headline; year column already uses derived `priorCampaign()`). tsc + build clean.
- ✅ **Development Overview — lapsed/new pledge fix + New Donors drilldown + Other tooltip + campaign YoY** (rev `ssrrbkcmdcenter-00678-q2c`, deployed + site UP HTTP 307). FIX 1: lapsed/new/retained now treat `gift_type IN (1,2)` as "gave" (new `fy26GaveDonors`/`fy25GaveDonors` sets) so a FY26 pledge no longer marks a donor lapsed; new-donor calc switched to FY26-not-FY25 set method (removed the old date-cutoff + `priorOperatingType1Donors`); last-gift maps now include type-2. Headline donor counts stay type-1-only (unchanged). Live: lapsed 179, new 1119, retained 786. FIX 2: New Donors headline card is now a clickable drilldown mirroring Lapsed (route payload gained `newDonors:{count,donors[]}`, org-excluded, amount-desc, top-100; drawer reused). FIX 3: "Other" segment card has an ⓘ hover tooltip explaining it (DAFs/foundations/untagged). FIX 4: campaign table last-year string now DERIVED (`priorCampaign()`, current−1) instead of hardcoded so it auto-rolls next FY; kept `gift_type IN (1,2)`; verified FY25 per-fund numbers already correct + reconcile with headline; deliberately did NOT add soft-credit gap-fill to the per-fund table (would break headline reconciliation). Only the overview route + OverviewTab touched. tsc + build clean.

- ✅ **Admin permissions — After School module column** (rev `ssrrbkcmdcenter-00676-k8w`, deployed + site UP HTTP 307). Added `after_school` ("After School") as a toggleable module column on `/admin/permissions`, after Absences. One entry in `DASHBOARD_COLUMNS` drives header + per-row toggle (shared `toggleModule` optimistic-update/revert/toast; grayed checkmark for owners/assistants, interactive for viewers via `PATCH /api/admin/workspace-members/[id]`). Also added to `ALL_MODULE_OPTIONS` (Add-User form). No other column/route/layout changed. tsc + build clean.

- ✅ **Development Overview — segment soft credits deduped by amount (2nd double-count fix)** (rev `ssrrbkcmdcenter-00674-x4g`, deployed + site UP HTTP 307). The gap-fill rule was correct but soft-credit-only donors still doubled: Veracross writes TWO soft-credit rows per gift (`soft_credit_type=1` AND `=2`) at the same amount, and the gap-fill summed both. E.g. Rosen (cid 8769): $110k pledge w/ type-3 sc=1 + type-3 sc=2 + type-5 sc=1, no type-1 → counted $220k, should be $110k. **Fix:** in the gap-fill branch, dedupe soft-credit rows by amount (each distinct amount once) — changed the per-donor soft accumulator from a running sum to a `Set<number>` of amounts in both `overview/route.ts` and `segment-donors/route.ts`. Unchanged: gap-fill rule, headline totals, Pledged sublines, Board Members priority, all other routes. tsc + build clean.

- ✅ **After School Programs — charts, search, student names, Veracross links, reorder** (rev `ssrrbkcmdcenter-00672-7jw`, deployed + site UP HTTP 307). Added to the After School page: (1) `level` column on `after_school_enrollments_cache` + sync now stores it — **finding: `level` is uniformly 0, not a pending/confirmed status**; (2) 3 recharts overview charts for the After School group only (Most Popular top-10 horizontal/blue, Enrollment by Day vertical/teal — day derived from name, Friday before Weekend per spec, Enrollment by Grade horizontal/purple); (3) class search filtering all 3 groups + auto-expanding sections with an "N results" count; (4) **real student names in the drilldown** via new `POST /api/after-school/students` — **had to use the admissions OAuth client, NOT programs** (programs token gets `401 missing scope: students:read` and can't request `students:*`; verified live), individual `/v3/students/{id}` in batches of 20 (id = person_id), graceful `{students:{}}` fallback, each student links to their Veracross record; (5) per-row + drilldown-header Veracross class deep links (`class-other-program/{id}/3156-general`, `e.stopPropagation()`); (6) drilldown grade chart (recharts, 180px); (7) section reorder After School → Tzaharon → MS (MS collapsed, "Registration opens Fall 2026–27" note). `GRADE_LABELS` upgraded to full labels. tsc + build clean. Touched only `AfterSchoolTab.tsx`, the sync route, the new students route, + one column — no other module.

## ✅ Shipped (June 15, 2026 — earlier)

- ✅ **After School Programs page — new module + Veracross programs API** (rev `ssrrbkcmdcenter-00670-97n`, deployed + site verified UP HTTP 307). New standalone Academics page (nav item after Student Absences, book-open icon) showing after-school enrollment from the Veracross **programs** API. New OAuth app creds `VERACROSS_PROGRAMS_CLIENT_ID/SECRET` (existing clients lack `programs.*` scopes — confirmed via `scripts/discoverProgramsApi.ts`); added to `.env.local` + `deploy.sh` Cloud Run env. Two cache tables (`after_school_classes_cache`, `after_school_enrollments_cache`, RLS on, server-only) created via `scripts/migrations/after-school-tables.sql`. Secret-auth sync route `POST /api/after-school/sync` (header-paginated; allow-lists the exact course names; classifies tzaharon / ms_extracurriculars / after_school; upserts classes **without** touching `capacity`; delete+reinsert enrollments so withdrawals drop out) — first run **595 classes / 5,417 enrollments**. Data route `GET /api/after-school?school_year=2026` (Supabase-only, `after_school`-module gated). UI `AfterSchoolTab.tsx`: year toggle 2025–26 / 2026–27 (default 26-27), 3 summary cards, per-group collapsible tables (enrolled color-coding, fill bars, grade chips), right slide-in per-class drilldown (grade breakdown + `Student #{person_id}` list — names stubbed). Daily 7am Cloud Function `syncAfterSchoolPrograms` (scheduler ENABLED). Module enabled on the SAR workspace; granted to Debra May (`debra@saracademy.org` — task said `debramay@`, which doesn't exist) and Becca (owner, sees it automatically). **Key learnings:** Veracross `school_year` = FALL year (2026 = AY 26-27); programs API paginates by `X-Page-Size`/`X-Page-Number` headers (default 2/page), no `X-Total-Count`; classes have no `capacity` field; enrollments have no names (only `person_id`); `meeting_times` needs a class id in the path. **Caveat:** owners/assistants (RBK, Emily) also see the page — inherent to the module model; per-user hiding would need a testing_features-style flag. tsc (app + functions) + build clean.

## ✅ Shipped (June 14, 2026)

- ✅ **Development Overview — segment soft credits now gap-fill only (double-count fix)** (rev `ssrrbkcmdcenter-00668-w2w`, deployed + site verified UP HTTP 307). Last week's change (`00659-9l8`) added type-3 soft credits to **every** constituent's segment `received`, but Veracross writes a `gift_type=3 / soft_credit_type=1` soft-credit twin for **every** direct `gift_type=1` gift — so every direct donor was counted twice, inflating all segment totals. Separately, donors whose only FY26 attribution was a household soft credit (`gift_type=3 / soft_credit_type=2`, e.g. Cory Greenbaum) were dropped from segments entirely. **Corrected rule (gap-fill), per constituent:** (1) has ANY type-1 row → segment `received` = SUM(type-1 amount), soft credits ignored (they're twins); (2) NO type-1 row → `received` = SUM(amount) on type-3 rows where `soft_credit_type IN (1,2)`. Soft credits ONLY fill the gap for non-direct donors; this also surfaces DAF/org- and household-only donors that were missing. Applies to segment totals, donor counts (a constituent counts once, in one segment, iff type-1 OR type-2 OR qualifying soft credit), and the drill-down lists (mirrored in `segment-donors/route.ts` so they reconcile). Files: `app/api/development/overview/route.ts`, `.../overview/segment-donors/route.ts` only. **Untouched** per spec: headline Total Raised / Total Donors (still `gift_type IN (1,2)`, type-1 count), per-fund campaign table, Lapsed/New/Retained pills, "Pledged" sublines (type-2 `pledge_balance`), Board Members→Trustee priority, all other tabs, `lib/syncGifts.ts`. **Deploy fix:** pinned `frameworksBackend.memory = "512MiB"` in `firebase.json` — the SSR function deploy was rejected because Cloud Run forbids `memory < 512Mi` with the live `--no-cpu-throttling` (CPU always-allocated) setting; now matches the `--memory=512Mi` the deploy script's gcloud step applies. tsc + build clean.

## ✅ Shipped (June 11, 2026)

- ✅ **Add-Task modal — "urgent" flag now sticks on creation** (rev `ssrrbkcmdcenter-00665-94w`, deployed + site verified UP HTTP 307). Creating a task with the urgent toggle on didn't persist; user had to reopen + re-toggle. Two bugs in the Create-Task handler in `app/components/Dashboard.tsx` (modal POSTs `/api/agenda-notes`; urgent is client-side localStorage, not a `/api/tasks` field): (1) it wrote `taskUrgent[\`note-${note.id}\`]` but all lookups key off `getTaskId()` = raw note id → fixed to `taskUrgent[note.id]`; (2) it never persisted to localStorage → now writes `localStorage.setItem('taskUrgent', …)` inside a functional updater, mirroring `toggleTaskUrgent`. Modal already had the urgent toggle UI. Untouched: card urgent toggles, the `taskUrgent` key name, task routes. tsc + build clean.

- ✅ **Buzz AI "Something's off on my end" intermittent-failure fix** (rev `ssrrbkcmdcenter-00664-7s9`, deployed + site verified UP HTTP 307). Four fixes: (1) **root cause** — Cloud Run was throttling CPU to ~0 after the 200 ack to Slack, freezing/aborting Buzz's fire-and-forget Anthropic call → error fallback. Added `--no-cpu-throttling` (+ required `--memory=512Mi`, was 256Mi) to the `gcloud run services update` step in `deploy.sh`; verified live annotation `cpu-throttling: false`. (2) **Per-stage logging** in `lib/buzzConversation.ts` main try (building context / loading history / calling Claude / saving history) + richer catch message, so future failures are diagnosable. (3) **Guarded `saveHistory`** in its own try/catch so a persistence error can't trigger a second "Something's off" after a good answer. (4) **Model id** `claude-sonnet-4-20250514` → `claude-sonnet-4-6` (project canonical). Untouched: fire-and-forget pattern, `processedEventIds` dedup, other Slack notifications. (`lib/morningBriefing.ts` still on the old model id — flagged for follow-up.) tsc + build clean.

- ✅ **Israel Fund "Total Raised" de-inflation — RE-VERIFIED (no new deploy)**. A follow-up request re-asked for the Israel Fund `gift_type = 1` filter + `raised`-cache recompute. **Already shipped earlier the same day (rev `00657-lpv`) and live.** Verified: `lib/syncIsraelFundRaised.ts` already has `.eq('gift_type', 1)` before summing; re-ran the idempotent `scripts/recompute-israel-fund-raised.ts` → **0 rows changed**, displayed Total Raised **$1,947,228.54** (in the $1.88M–$1.95M band); `seed_raised` / `manual_raised` / `is_excluded` untouched. Skipped a redundant redeploy (no code delta = no-op revision). tsc clean.

- ✅ **Development Overview — Lapsed Donors list excludes organizations** (rev `ssrrbkcmdcenter-00661-2hs`, deployed + site verified UP HTTP 307). The Lapsed Donors drill-down was listing orgs (DAFs/foundations/charitable funds); only individual persons should appear. In `gifts_cache` the Veracross record type lives in `raw_data->>'constituent_record_type'` (`'2'` = person, `'3'` = org). Fix in `app/api/development/overview/route.ts`: after building `lapsedIds`, a chunked `gifts_cache` query filtered `.eq('raw_data->>constituent_record_type','3')` collects org IDs (checking **any** gift row for the constituent, not just Operating 2025-2026 — a lapsed org may have no current-year row), then those are filtered out of the displayed `lapsedDonors` array. Unknown/missing record types are kept (only explicit `'3'` excludes). **Lapsed COUNT pill unchanged** (still full `lapsedIds.length`) — only the drill-down list is person-filtered, per spec. tsc + build clean.

- ✅ **Development Overview — Board Members segment + soft credits in segment cards** (rev `ssrrbkcmdcenter-00659-9l8`, deployed + site verified UP HTTP 307). (1) Added **Board Members** (trustees) as the **highest-priority** segment in `classifySegment` (`roles_raw ILIKE '%Trustee%'` AND NOT `%Trustee - Former%` AND NOT `%DECEASED%`) — so a trustee who is also a parent now classifies as Board Members. New order: Board Members → Parent → Grandparent → Parents of Alumni → Program & Future Families → Alumni → Faculty → Other (UI segment grid widened 7→8 cols, indigo card style). (2) **Soft credits** (`gift_type = 3`, `soft_credit_type = 1`) now count toward each **segment card** total + donor count (DAF/foundation gifts were falling into Other) — folded into the card "Received" subline (type-1 + type-3) and `fy26SegmentDonors`; "Pledged" stays type-2 only. **Headline Total Raised / Total Donors / per-fund campaign table / Lapsed / New are unchanged** (still `gift_type IN (1,2)`). Drill-down route mirrors the same math so donor lists reconcile with card totals. Files: `app/api/development/overview/route.ts`, `.../overview/segment-donors/route.ts`, `app/components/development/OverviewTab.tsx`. tsc + build clean.

- ✅ **Israel Fund "Total Raised" de-inflated** (rev `ssrrbkcmdcenter-00657-lpv`, deployed + site verified UP HTTP 307). The raised-cache incremental sync (`lib/syncIsraelFundRaised.ts`) was summing **all** `gift_type`s from `gifts_cache`, so non-seeded events double-counted Veracross's hard-credit (`gift_type=1`) + soft-credit (`gift_type=3`) row pairs (plus leaking pledges types 2/5). **Code fix:** added `.eq('gift_type', 1)` before the amounts are aggregated. **Data fix:** one-time `scripts/recompute-israel-fund-raised.ts` reset `raised` for non-seeded rows only to the `gift_type=1` sum (9 rows lowered). `seed_raised` ($1,841,649.79 across 39 seeded rows), `manual_raised` ($50,697), and `is_excluded` left untouched. Displayed Total Raised: **$2.05M → $1,947,228.54** (in the expected $1.88M–$1.95M band). Script is idempotent.

---

## ✅ Shipped (June 10, 2026)

- ✅ **Major-gift Slack alert no longer double-fires** (rev `ssrrbkcmdcenter-00655-x2t`, deployed + site verified UP). Veracross writes a hard credit (`gift_type=1`) AND a soft credit (`gift_type=3`) per gift, so Sara's ≥$1,000 alert listed each gift twice. Added `gift_type === 1` to the `majorGifts` filter in `lib/syncGifts.ts` (hard credits / real money only; pledges type 2 + soft-credit pledges type 5 also excluded). `gifts_cache` upsert unchanged — still stores all gift types.

---

## 🔴 Immediate (This Week)

- [ ] **Cooper Fund — Schreck + Israel Gap Year merge**: `Israel Gap Year Scholarships` + `M Schreck B Ball Tournament` should both display as `M Schreck Fund / Israel Gap Year Scholarship`. Cooper still shows them split.
- [ ] **Cooper Fund — General/Undesignated → Cooper 25-26 bucket**. Emily asked where the $360 is pulling from.
- [ ] **Cooper Fund — Cooper Yahrzeit → Cooper 25-26 bucket**.
- [ ] **Cooper Fund — drop Education Sayeret Matkal Soldier entirely** (Israel Fund, not Cooper).
- [ ] **Cooper Fund — Cooper Purim 25 raised should be $0**. Money raised was last FY, disbursements are this FY. Confirm with Emily; she may relabel in the spreadsheet.
- [ ] **Cooper Fund accuracy gap**: Leora's ground truth = $350,294; page shows $388,804 → $38K gap. Root cause TBD.
- [ ] **Cooper Fund pie chart label overlap fix** — prompt drafted, deploy pending.
- [ ] **Cooper Fund — General Fund summary card** to left of bar chart — prompt drafted, deploy pending.
- [ ] **Israel Fund — Emily verifies seeded grant totals** match her spreadsheet ground truth. 346 rows imported; numbers should match.
- [ ] **Israel Fund — drop `total_paid_out` + `paidOut` aliases** from `/api/development/israel-fund` response (UI now uses `total_disbursed` / `disbursed` exclusively).

---

## 🟡 Development Module

### Guardian Circle
- [ ] **Migrate Veracross link to `app.veracross.com/sar/{person_id}/273/general`** — current link uses `development-constituent` axiom path with `constituent_id` (works). Move requires `person_id` in `constituents_cache`; not currently synced.
- [ ] **Mobile column overflow** — Guardian Circle + Weekly Gifts donor tables: show only Name + Event/Fund on mobile, all others `hidden md:table-cell`; row tap opens existing sidebar.

### Cooper Fund / Israel Fund
- [ ] **"Today" filter pill** on both Cooper Fund and Israel Fund money-in sections (same first-pill pattern as Weekly Gifts).
- [ ] **Clickable event rows** on both pages: expand a money-in row to show individual gifts (constituent name, amount, date, Veracross link) + DonorAnnotations per gift.
- [ ] **Cooper Fund Slack channel mention** — `@CooperFundChannel` taggable in the @mention dropdown on Cooper Fund notes (blocker: channel ID from Emily).
- [ ] **Cooper Fund YoY category tracking** — current FY Column H vs prior year snapshot from `cooper_fund_categories`.
- [ ] **Source spreadsheet link** on Cooper Fund + Israel Fund pages (Heidi's suggestion), permission-gated.

### Overview / homepage
- [ ] **Lapsed Donors card** — decide whether to hide until Shavuot per Becca's note.

### Capital Campaign
- [ ] **Build out Capital Campaign tab content**. Fund: `Big Bold Future`. Sara sending Veracross queries — UNBLOCKED. Wants: overview buckets (designated vs undesignated), YoY, by constituent.

### Year-over-Year
- [ ] **Add last-year data alongside current year across all Development metrics** — total raised, donors by segment, lapsed count. Sara has 5+ years of data; can start with static/hardcoded prior year then build the snapshot system.

### Confirm with Leora
- [ ] Verify new Veracross events auto-show on Command Center with no manual step.

### Veracross API blocker (assigned to Veracross)
- [ ] **Veracross ticket: `/v3/development/gifts` historical gap** — full pull returns ~11k rows, earliest 2025-04-27. ~$703K Israel Fund history + 3 known Guardian Circle gifts ($28K) inaccessible via this endpoint. Ticket submitted 2026-06-03. Update `FLAGGED_RECORDS` in `GuardianCirclePage.tsx` as gifts resolve.

---

## 🟡 Other Modules

### Tasks
- [ ] **Due-Date Slack reminder** — Cloud Function at 8am ET → query `tasks WHERE due_date = today AND status != 'done'` → DM each assignee's Slack ID from MENTIONABLE_USERS.

### Simchas & Shivas
- [ ] **Send Note overhaul** — modal/inline form first with optional note text → task body (not just title) in Emily's queue with Hamakum-parsed family info + RBK's note → Slack DM Emily on task creation.

### Emails Page + Thank You workflow
- [ ] **"Action Items" / "To Send" section at top of All Emails** with three card types: Day of Learning thank yous, Guardian Circle thank yous (surface envelope-icon drafts), Condolence notes from the Simchas flow. Each card: donor/family, type, draft preview, Send + Edit.
- [ ] **Day of Learning thank you automation** — fund `OP: Sponsorships`, event `Day of Learning 25-26`. Add Veracross gift-notes (dedication text) to `gifts_cache` sync → auto-draft via Claude → surface in To Send cards. Leora maintains `day_of_learning_month` / `day_of_learning_day` profile codes.
- [ ] **Auto-BCC Veracross tracking on all thank yous sent from the app** — append `bcc=` to mailto/Gmail send payload.

### Calendar / Week at a Glance
- [ ] Verify Zoom/Meet/Teams link detection in prod (added 6/2).
- [ ] RSVP / Register link on calendar event cards if event has registration URL.
- [ ] Confirm calendar event grade tags working for all recurring events.

### Student Absences
- [ ] Confirm correct Veracross query ID for "Yesterday's Absences" — current link is placeholder `1473847`.
- [ ] User-education note: 8:15am high present rate is expected (default present until teacher submits). Discuss with Emily Gray before any code change.

### Attendance YTD
- [ ] Perfect-attendance students missing from tiers chart (not in `attendance_cache` by design).

### Communications
- [ ] Get Yael + Ilana into a meeting (Emily to push). RBK explicitly wants to use Communications.
- [ ] Monday.com Approvals Queue: raw API status value mismatch with `"Pending RBK Approval"`.

### Recruiting
- [ ] **Lever webhook activation** — manual step in Lever Settings → Webhooks. Triggers Slack DM to RBK for new non-HS applications.
- [ ] Walk RBK through the Lever page + notes flow (no build, just demo).
- [ ] **Recruiting HS toggle for Debra May** — Academy / HS / All (same pattern as enrollment).

### Admissions
- [ ] **Application Pipeline tab** — prospects (inquiry) vs applicants (full application). Blocked on Veracross field investigation.

### Infrastructure / cleanup
- [ ] Cloud Function workspace-awareness (reads credentials from DB vs env vars) — Phase G.
- [ ] Credential encryption (`pgsodium` / KMS for `workspace_integrations`) — Phase G.
- [ ] Google Tasks two-way sync (auth flow already scaffolded).
- [ ] Phase A-F regression test pass — RBK email signature, division filters, Tasks columns, Inbox sections, social tiles, geography drilldown.
- [ ] Weekly Gifts: filter out `constituent_name = 'SAR Academy'` internal $900K journal entries (1-line fix).

---

## 🟠 Blocked / Waiting on Stakeholders

### Debra May (Executive Director)
- [ ] **Transportation page** — bus route → stop mapping. Page shows student name, grade, stop, cost, outstanding balance. P&L per route. Academy + HS.
- [ ] **Scholarship button** — ParentLocker URL format per student. Button visible to all (privacy); only works for authorized.
- [ ] **After-school programs page** — Veracross endpoint. Enrollment per program + grade, drilldown like admissions.

### Joe Ali (Facilities/IT)
- [ ] **Facilities/IT ticket tracker** — Joe getting shared access to ticketing system. Surface (link or iframe) — don't rebuild.

### Leora Miller (Development)
- [ ] Day of Learning thank yous: Veracross field name for dedication/honoree text.

### Emily Gray (EA)
- [ ] Thank-you BCC email address for Veracross gift tracking.
- [ ] Cooper Fund Slack channel ID for `@CooperFundChannel` mention.

### Sara Hasson (Development)
- [ ] Veracross queries for Big Bold Future / Capital Campaign tab data.
- [ ] Campaign naming cleanup in Veracross — RBK wants fewer top-level campaigns with subcategories.

### Paycom (HR vendor)
- [ ] API credentials. Email sent to rep; awaiting response. Short-term workaround: Amy's Google Sheet (already populated by GAS scraping Paycom Slack messages).

### RBK
- [ ] Rise Vision / Digital Signage — screen URLs for 5–6 campus displays.
- [ ] App name decision — Becca + Debra May both dislike "Command Center". Becca suggested "Beacon" (Veracross uses that too). Ask RBK directly.

### Becca to gather
- [ ] HS principal email, name, title — for onboarding row in `workspace_members`.

### Veracross support
- [ ] `/v3/development/gifts` historical gap (ticket 2026-06-03).
- [ ] `grade_applying_for` / `student_group_applying_for` for re-enrolling students (second follow-up needed).

---

## 🟣 Non-Code Items

### Permissions to grant
- [ ] **Debra May**: add Development module + `guardian-circle` access via `/admin/permissions`.

### Manual steps
- [ ] **Lever webhook**: activate in Lever Settings → Integrations & API → Webhooks. URL `https://rbk-cmd-center.web.app/api/webhook/lever`, `applicationCreated` event.
- [ ] **Veracross ticket**: file ticket on the `/v3/development/gifts` historical gap if it hasn't been logged in their support system yet (initial outreach 2026-06-03).

### Sidebar / nav decisions
- [ ] Rename sidebar group "Community" → "Academics" (Simchas to move out of that group).
- [ ] Add **Student Logs page** to sidebar under Academics — link to Veracross Axiom query #1473847.

### Meetings needed
- [ ] Yael + Ilana (Communications team) — Emily to push.
- [ ] Sara Hasson — Capital Campaign + Big Bold Future query review.
- [ ] HS principal (when onboarded) + RBK — HS dashboard expansion proposal.

### Long-tail other
- [ ] Shabbos print queue concept — RBK drops article links during the week; Emily prints Friday.

---

## ✅ Completed Today (June 5, 2026)

_No new deploys today yet. The most recent ship was 2026-06-03 (`ssrrbkcmdcenter-00600-r46` — Israel Fund canEditGrants role-clause cleanup). Today's session is documentation only — Vision + Roadmap rewrite and master TODO restructure._

---

## ✅ Recently Shipped (last ~2 weeks)

_Items resolved more than two weeks ago have been pruned and now live in `CLAUDE_CONTEXT.md` Recent Changes._

- ✅ **Israel Fund — `israel_fund_editor` sub-permission** (`00598-4zw` → `00600-r46` cleanup, 2026-06-03). First real call site of `hasSubPermission()` in the codebase. Replaced hardcoded `isEmily` with sub-permission + email fallback. SUB_PERMISSIONS registry gained the entry; Becca grants new editors via /admin/permissions Users tab.
- ✅ **Israel Fund — grant CRUD UI** (`00594-lk8`, 2026-06-03). `IsraelFundTab.tsx` uses `total_disbursed` / `disbursed` everywhere (UI rename from `paidOut`). Expandable initiative table with sub-grants, viewer vs. editor column split, 480px Add/Edit slide-in panel with combobox initiative picker, Recharts horizontal bar chart.
- ✅ **Israel Fund — initial seed run** (2026-06-03). 346 grants imported via `npm run seed:israel-grants` from `data/master-eg.csv`. npm script switched from `ts-node` → `tsx` to fix `__dirname` under ESM.
- ✅ **Israel Fund — grants moved to first-class Supabase table** (`00592-2zt`, 2026-06-03). New `israel_fund_grants` schema + RLS; CRUD API (POST/GET list, PATCH/DELETE single); seed script `scripts/seed-israel-fund-grants.ts`. `fetchIsraelGrantsPaidOut` (Sheets) helper retained for rollback but no longer called.
- ✅ **Israel Fund — three fixes** (`00590-2f7`, 2026-06-03). FIX 1: grants sheet is dynamic whitelist (drops unrelated Veracross events). FIX 2: raised total switched to cash-received gift types `[1, 3, 5]`; deviates from Cooper's gift-type model (flagged in route comments). FIX 3: backfill route accepts `?start_date=YYYY-MM-DD` with client-side filter (Veracross rejected `date_from` and `query_string` params).
- ✅ **Guardian Circle — flagged-gifts data-quality callout** (`00584-mlk`, 2026-06-03). Amber collapsible at the top, data-driven from `FLAGGED_RECORDS` array; current total $28,671 across 3 gifts the Veracross API drops.
- ✅ **Israel Fund — live Money Out from Emily's grants sheet** (`00582-48g`, 2026-06-03). Initial Google Sheets pipeline (superseded later same day by the Supabase table). Money In filter widened to `fundraising_activity ILIKE '%External Funds%'`. `israel_fund_summary` snapshot table no longer read.
- ✅ **Feature Flags admin tab** (`00580-bcg`, 2026-06-03). `workspaces.promoted_features text[]` column + `canSeeTestingFeature()` helper. One-click Promote to Live (with confirm) / Move back to Testing.
- ✅ **Testing & Preview feature-flag system** (`00578-vf9`, 2026-06-03). `workspace_members.testing_features text[]` + `TESTING_FEATURES` registry + per-member 🧪 section in Admin → Users. Development Overview tab is first gated feature.
- ✅ **Development Overview landing tab** (`00570-9rz`, 2026-06-02). Default Development tab. Headline cards, segment cards by role, Campaign Giving YoY table, Lapsed Donors collapsible (top 100).
- ✅ **Guardian Circle — `constituents_cache` sync** (`00568-25k`, 2026-06-02). Veracross `/v3/development/constituents` + `/v3/students` joined nightly. 9,650 constituents → 4,213 with specific role, 911 with grade data, 89 flagged aging-out.
- ✅ **Guardian Circle — Sprint 4** (`00562-gfm`, 2026-06-02). BBF / Capital column, role pill + grade chips + aging-out 🚩, clickable donor name → sidebar drawer.
- ✅ **Guardian Circle — Veracross link fix** ("person record not found" bug, 2026-06-02). Switched to `development-constituent` / `organization-constituent` axiom path with `constituent_id`.
- ✅ **Guardian Circle — donor-count fix** (~250 → unique non-org donors, 2026-06-02).
- ✅ **Cooper Fund — event consolidation** (M Schreck variants, Cooper Yahrzeit + General → Cooper 25-26, Education Sayeret Matkal dropped) — 2026-06-02.
- ✅ **Cooper Fund — live Google Sheets Column G** (no hardcoded data, 2026-06-02).
- ✅ **Cooper Fund — DEV: / non-APL event prefix fix** (no longer filtering out, 2026-06-02).
- ✅ **Cooper Fund — Cooper 25-26 split into General Fund summary card** (2026-06-02).
- ✅ **Cooper Fund — bar + pie chart polish** (2026-06-02).
- ✅ **Student Absences — Academy-only at API layer** + grade-card layout (`00558-2kg`, 2026-06-02). HS grades dropped regardless of caller divisions; click card → expanded roster.
- ✅ **Admissions Pass 2 verified live** + ELC/LS card recolor (teal/blue/purple) — 2026-06-02.

---

*Note: "Debra Eis" = Admissions director feedback. "Debra May" = Executive Director feedback. Different people.*
